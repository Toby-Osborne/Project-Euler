#include <vector>
#include <iostream>
#include <numeric>
#include <bits/stdc++.h>
#include <cmath>
using namespace std;


int main(){
    clock_t begin = clock();
    double time_spent = 0.0;
    int x = 1;
    bool prime;
    long long int sum = 2;
    forward_list <int> primes;
    int count = 1;
    
    for (long long int x = 3;x<) {
        for (int i = 0;i<count;i++) {
            
        }
    }

    cout << sum << endl;
    clock_t end = clock();
    time_spent += (double)(end - begin) / CLOCKS_PER_SEC;
    cout << "Time Spent: " << time_spent << endl;
    return 0; 
}